# Binaries
# Go build artifacts
bin/
build/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
/picoclaw
/picoclaw-test
/picoclaw-saas
/picoclaw-launcher
/picoclaw-tenantctl
cmd/**/workspace

# Dev / debug artifacts at repo root
.codex-dev-logs/
pre-cadastro-e2e-*.png

# agent-skills + ui-ux-pro-max-skill were accidentally added as gitlinks
# (mode 160000) in earlier commits without a corresponding .gitmodules
# entry. That broke CI's submodule checkout step. Removed from the index
# in PR #39; ignored here so they don't sneak back in. If you want to
# track them for real, either:
#   - add as a regular directory: `git add -f <name>/`
#   - add as a proper submodule:  `git submodule add <url> <name>`
agent-skills/
ui-ux-pro-max-skill/

# Runtime state under workspace/ that should never be tracked
workspace/state/
workspace/whatsapp/
workspace/sessions/
workspace/matrix/
workspace/runtime-user-env/
workspace/heartbeat.log
workspace/*.log

# Picoclaw specific

# PicoClaw
.picoclaw/
config.json
sessions/
build/

# Coverage

# Secrets & Config (keep templates, ignore actual secrets)
.env
.env.local
.env.*.local
docker/saas/.env.supabase.local
config/config.json
.security.yml
onboard


# Test
coverage.txt
coverage.html

# OS
.DS_Store

# Ralph workspace
ralph/
.ralph/
tasks/

# Plans
docs/plans/
docs/superpowers/

# Editors
.vscode/
.idea/

# Added by goreleaser init:
dist/
!internal/saas/api/dist/
!internal/saas/api/dist/.gitkeep
*.vite/

# Windows Application Icon/Resource
*.syso
.cache/
web/frontend/.pnpm-store/
_tmp_*
web/frontend/_tmp_*

# Test telegram integration
cmd/telegram/

# Keep embedded backend dist directory placeholder in VCS
!web/backend/dist/
web/backend/dist/*
!web/backend/dist/.gitkeep

.claude/

docker/data

.omc/
.claude-*.zip
.playwright-mcp/
docker/saas/traefik/dynamic/dev-tls.yml
