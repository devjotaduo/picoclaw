global:
  checkNewVersion: false
  sendAnonymousUsage: false

log:
  level: INFO

accessLog: {}

api:
  dashboard: false
  insecure: false

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    address: ":443"
    transport:
      respondingTimeouts:
        # SSE streams pra Sofia/Catarina (em /api/public/chat/stream)
        # ficam abertos enquanto durar a sessão do visitante. Cada turn
        # da Sofia via claude-cli demora 60-180s; com o default de 60s
        # do Traefik v3 (readTimeout), a connection era fechada
        # exatamente quando a resposta longa ia chegar — saudação rápida
        # da Sofia funcionava, mas qualquer turn com tool call cortava
        # no meio (validado live em prod 2026-05-28: SSE puro fechava
        # em exatos 61s).
        # 0 = no limit. O cap downstream é o WriteTimeout do gateway
        # HTTP server (pkg/channels/manager.go, 10min) que mata zumbis.
        readTimeout: "0s"
        idleTimeout: "0s"

providers:
  docker:
    # Mount /var/run/docker.sock directly. The dockerproxy v0.3 strips the
    # Docker API version negotiation headers, which makes Traefik fall back to
    # API 1.24 — rejected by Docker daemon >= 29.x (MinAPIVersion 1.40).
    # The proxy is still in front of the controlplane for tenant create/start.
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: saas_edge
    watch: true
  file:
    directory: /etc/traefik/dynamic
    watch: true

certificatesResolvers:
  letsencrypt:
    acme:
      # Hardcoded for the Jotaduo deployment — Traefik does NOT interpolate
      # env vars inside the static YAML (so `${LE_EMAIL}` would be sent
      # literally to Let's Encrypt and rejected as "unable to parse email").
      # If you fork this stack, swap this to your own contact address.
      email: "dev@jotaduo.com"
      storage: "/letsencrypt/acme.json"
      # HTTP-01 challenge: works without a Cloudflare DNS API token. Each
      # tenant subdomain gets its own cert lazily — the tenant-router
      # systemd watcher (docker/saas/scripts/tenant-router/) writes a
      # concrete Host() router per running tenant so Traefik knows what to
      # request a cert for. Wildcard via DNS-01 would need CF_DNS_API_TOKEN.
      httpChallenge:
        entryPoint: web
