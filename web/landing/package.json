{
  "name": "@picoclaw/landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Static React landing page served at jotaduo.com (apex). Built locally and uploaded to /var/lib/picoclaw-landing on the VPS via scp/rsync — NOT embedded into the controlplane binary, so iterating doesn't require a Go rebuild.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
