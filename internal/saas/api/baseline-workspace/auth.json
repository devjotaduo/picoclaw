{
  "_comment": "OAuth credentials per provider. Empty by default — the launcher writes here when an operator runs `picoclaw auth login <provider>` inside the tenant container. If /etc/picoclaw/shared-auth.json exists on the controlplane host, the provisioner copies its contents into this file at tenant create time so operators only need to authenticate once at the platform level.",
  "providers": {}
}
